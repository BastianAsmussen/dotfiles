# Agent Skills

Skills are specialized instruction sets. Load them with `skill <name>` before starting complex work.

Use `using-agent-skills` to discover which skill applies. References in `~/.config/opencode/references/` — load when domain applies.
